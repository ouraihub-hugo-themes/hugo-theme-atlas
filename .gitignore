node_modules/
public/
resources/
.hugo_build.lock
exampleSite/public/
exampleSite/resources/

# 编辑器的本地配置。它属于开发者的机器而不是这个主题 —— 提交进来的话每个人
# 打开仓库都会被改一次。
.zed/
.vscode/
.idea/

# 浏览器验证的工具产物。Playwright 每次导航都往当前工作树写一份页面快照与
# 控制台日志，截图默认也落这里 —— 全是一次性的诊断输出，跟主题无关。
# 之前靠每轮手动清，清干净过但那是运气：漏一次就进提交。
.playwright-mcp/

# 验证截图。约定是落在仓库外（免得混进提交），这条是万一约定没守住时的兜底。
shots/

# assets/dist/ 是编译产物，但要提交 —— 用户直接消费，不装 Node。
# 不参与 diff，见 .gitattributes。

# sourcemap 例外：只服务本地 pnpm dev，消费者拿不到 src/ 也用不上，
# 而且体积是产物本身的三倍。
*.map
