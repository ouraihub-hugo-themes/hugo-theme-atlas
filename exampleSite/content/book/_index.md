---
title: 手册
type: book
reading_width: slim
weight: 20
description: book 壳：与 docs 同一副骨架，差别只在阅读量度。
---

这一段在 `book` 壳里。它和 `docs` 用同一份 baseof、同一个 grid、同一套侧栏与
TOC —— 四个壳之间的实际差异只有一个标记类和 `reading_width`，不值得各留一份
baseof。

本节 front matter 写了 `reading_width: slim`，所以正文列被收到 48rem。
翻到子章节看 `normal` 和 `wide` 的对比。
