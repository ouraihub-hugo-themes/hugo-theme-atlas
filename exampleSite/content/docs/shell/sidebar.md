---
title: 侧栏
description: 嵌套树、折叠状态、当前行标记。
weight: 10
---

## 树结构

侧栏用 `<details>` 承担折叠。原生 disclosure 自带键盘可达与辅助技术语义，
写 JS 折叠等于重新实现浏览器已有的东西。

## 折叠状态

初始状态由服务端按读者的位置渲染：祖先链上的 section 展开，其余收起。没有
JS 时这份状态就是全部 —— 原生 `<details>` 自己会折叠，只是翻页后回到服务端
那一份。

运行时在此之上记住**读者手动展开过哪些**，写进 `localStorage`。这份记录只加
不减地作用于恢复：只打开，绝不关闭。

这个不对称是有意的。服务端每翻一页都会重新展开当前所在的那条链，如果让存储
覆盖它，读者正在读的 section 会在导航后自己收起来 —— 人就找不到自己在哪了。
所以：

- 手动展开一个服务端收起的 section，跨页保留
- 关掉一个自己展开过的 section，跨页保留（从集合里移除）
- 关掉自己**正在读**的那条链，只在当前页有效，下一页服务端会再展开它

## 状态色

正在阅读的那一行底色是 `--td-shell-selected`（accent 14%），hover 是
`--td-shell-wash`（accent 7%）。两者差一倍，所以一串 hover 痕迹不会和读者
真正所在的那行竞争。

## 缩进

子列表用 `padding-inline-start`，逻辑属性在 RTL 下自动翻面。
