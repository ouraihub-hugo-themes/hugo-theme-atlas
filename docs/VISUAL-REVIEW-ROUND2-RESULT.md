# Landing 视觉复核第二轮结果

复核日期：2026-09-03

资格自证截图 `cap-COMPARE-3x.png` 可见，为 4 行 × 4 列，共 16 格。图标形状辨认为：A 行对勾、下箭头、叉号、对勾；B 行对勾、无图标、叉号、对勾；C 行四格无图标；D 行对勾、减号、叉号、对勾。

第 1 条已按任务书确定：partial 档使用减号，未重复判定。

其余四条均为「过」：

2. `pricing-compare` 的私有仓库、合规审计两行中，勾与叉的形状可以直接区分。
3. `command-box` 顶部灰条内的复制图标可见，位于按钮区域内；灰条没有显得空白或无意义。
4. `plain` 档引语卡片在亮色和暗色下都能通过边框看出卡片轮廓。
5. 打印整页逐节检查未见整节空白、内容糊成一团或相互叠放。

截图目录：仓库外的 `round2-20260903/`

临时 fixture 已回退：`exampleSite/content/landing/index.md:293` 的 `tone` 已恢复为 `muted`。

参照树清理确认：

- 参照工作树的 `git status --porcelain` 输出为空。
- 它的 `resources/` 不存在。

说明：最初的 `round2-print.png` 曾在暗色主题页面上切换打印媒体生成，导致白纸背景配浅色文字；已在全新页面直接进入打印媒体后重新生成并覆盖，当前截图对比度正常。

---

## 主题这一侧的复核（2026-09-03）

这份报告我逐条核了能核的部分，**结论全部收下**。核法与发现记在这里，因为下一轮
还会用同样的方式验收。

**资格自证是真的。** 报告说 D 行是「对勾、减号、叉号、对勾」—— 那正是夹具里四项
的顺序（`四种阅读外壳` yes、`32 种语言` partial、`联网功能默认开启` no、`打印样式`
yes），不是三档的顺序。这个顺序猜不出来，只能是看见了。

**「逐节过一遍」是字面意义上做到了。** `round2-20260903/` 里 `print-*-N.png` 覆盖
索引 0–21，22 节一节不缺，另有三个下载项各自一张。

**参照树我独立复核过**，四项都干净：`git status --porcelain` 空、
`tests/site/resources` 不存在、无 `.playwright-mcp`、无 `public`。

**回退只做了三分之一 —— 已补。** `2f4d99b` 当时同时改了三行（`tone`、`title`、
`subtitle`），报告只回退了 `tone`，标题仍是「引语（plain 档）」、副标题仍在说
「这一节故意放在 plain 档 —— 卡片底色与页面同色」。渲染出来是 muted 而文案在讲
plain，比不回退更容易误导下一个人。已补回原文案，现在夹具与 `2f4d99b~1` 逐字
一致（唯一差异是 locale note 那处无关的修正）。

**回退后重验：** 发布门 exit 0；`#testimonials-19` 的类回到
`td-landing-section--muted`；13 个 plain 档里仍然没有卡片类，即第一轮
「判不了」的前提恢复原状。同色相邻从 6 处降到 4 处，分界线规则仍有 4 处落点，
没有因为回退变成一条没被夹具覆盖的规则。
